<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:feeds, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:feeds, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:feed) %></h2>
    <div class="inner">
      <% form_for :feed, url(:feeds, :create), :class => :form do |f| %>
        <%= partial "feeds/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>