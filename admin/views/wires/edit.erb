<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:wires, :index) %></li>
      <li><%= link_to pat(:new), url(:wires, :new) %></li>
      <li class="active"><%= link_to pat(:edit), url(:wires, :edit, :id => @wire.id) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:edit) %> <% mt(:wire) %></h2>
    <div class="inner">
      <% form_for :wire, url(:wires, :update, :id => @wire.id), :method => :put, :class => :form do |f| %>
        <%= partial "wires/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>