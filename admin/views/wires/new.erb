<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:wires, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:wires, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:wire) %></h2>
    <div class="inner">
      <% form_for :wire, url(:wires, :create), :class => :form do |f| %>
        <%= partial "wires/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>